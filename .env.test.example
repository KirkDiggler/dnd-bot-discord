# Test Environment Configuration
# Copy this to .env.test for test-specific configuration

# Use a different Redis database for tests to avoid wiping dev data
REDIS_URL=redis://localhost:6379/15

# Discord token for integration tests (if needed)
# DISCORD_TOKEN=your-test-bot-token

# Set this to run integration tests
INTEGRATION_TEST=true